# 03 — 作者参与（并行插入 + @ 召唤）

**What to build:** 作者作为群成员随时发送消息：消息实时并行插入、不阻塞当前 Agent 生成，并在下一个发言权窗口被优先处理；支持 @角色名 定向召唤（被 @ 者 +100 强意愿加成，下轮大概率由被 @ 者发言）；前端支持点击成员列表快速 @；作者发言计入讨论历史与上下文，后续 Agent 会承接作者意见。

**Blocked by:** 02

**Status:** resolved

- [ ] 作者在 Agent 生成中发消息：消息立即上屏、当前生成不被中断（黑盒断言：并行插入时序正确）。
- [ ] @ 角色后，下一个发言权窗口归被 @ 者（黑盒断言）。
- [ ] 前端支持手打 @ 与点击成员快速 @。
- [ ] 作者消息进入上下文，后续 Agent 发言引用 / 承接作者意见。

## Answer

已完成。

- 后端：sendUserMessage 解析消息中的 @角色名 并调用调度器 mention（+100 强意愿加成，无视冷却优先），多个命中角色均可被召唤；作者消息并行插入、实时上屏（工单 01 已具备），并计入上下文与话题相关性。
- 测试：新增 2 例黑盒断言——作者 @ 某角色后下一发言窗口归被 @ 者（得分 mention=100，并覆盖「无 @ 时 tie-break 会是另一人」的判定）；作者消息计入上下文、后续 Agent 发言 replyTo 指向作者消息。chat_session.test.ts 共 15 例全部通过。
- 前端：GroupChatView 成员面板支持点击 Agent 成员快速插入 @角色名 到输入框（悬停提示 + 光标指针 + 输入区「点击左侧成员可快速 @」提示）。
