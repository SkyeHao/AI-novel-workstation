# 任务票 01-backend-skeleton：01-backend-skeleton

Type: task
Status: resolved
Blocked by:

## Question

搭建 TS 后端骨架：package.json(npm/pnpm) + TypeScript + Fastify + vitest；目录结构（src/llm src/storage src/agent src/api）；tsconfig；CI 脚本。

## Answer

TS 后端骨架完成：backend/ 目录 + package.json(npm) + TypeScript strict + Fastify 5 + vitest 2；tsc 编译通过，health 冒烟测试绿。BOM 问题已处理（JSON 无 BOM）。

