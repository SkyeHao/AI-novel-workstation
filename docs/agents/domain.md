# Domain Docs

Single-context repo: one `CONTEXT.md` at root + `docs/adr/`.

- Read `CONTEXT.md` and relevant `docs/adr/*` before exploring code.
- Use glossary vocabulary from `CONTEXT.md` in all output.
- Flag ADR conflicts explicitly rather than silently overriding.

## Reports

- Agent 内部逻辑分析报告（系统提示词/上下文编排/记忆召回/ReAct 循环/会话持久化）：
  `docs/reports/agent-internal-analysis.md`
