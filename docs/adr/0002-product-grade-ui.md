# UI 以产品级标准设计

重绘的项目是面向有计算机基础用户的**前端优先产品**，用户全程只与界面交互。因此 UI 按产品级标准做，而非工具级堆表单：

- 人物卡片设计为**可点击的卡片组件**（翻转/展开详情），而非表格行或纯文本
- 世界观、大纲等设定有结构化的可视化呈现，而非只有 Markdown 编辑框
- 阅读页、IDEATION 对话页同样追求精良视觉与流畅交互

配套 Q10-A：底层结构化 JSON 存储 + 前端双视图（表单/卡片编辑 + Markdown 预览）。
