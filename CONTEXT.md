# AI 小说工作站

一个面向"有一定计算机基础的用户"的前端优先 Web 应用，通过 AI 辅助完成长篇网络小说创作。用户全程只与前端界面交互，创作数据与代码分离存放。

## Language

**小说工作站**:
本产品。以"人主导 + AI 辅助"为原则的长篇网络小说创作工具。本地单机运行。
_Avoid_: llm-client-tester、通用聊天工具、通用 Agent

**IDEA**:
用户最初提供给 AI 的创意火花，是创作流程的起点。由 AI 扩展、与用户对话确认。
_Avoid_: 点子、想法、需求

**创意共创**:
由 IDEA 出发、与作者多轮对话确认核心要素的流程。Agent 在对话中逐步提问，作者逐个拍板；要素确认后系统询问是否生成愿景文档，生成完成后询问是否继续生成设定。Agent 的 done=true 表示"本阶段完成"，前端据此渲染下一步操作卡片。
_Avoid_: IDEATION、灵感阶段、策划对话

**核心要素**:
小说动笔前必须与用户确认的内容——主线、愿景、风格、世界观。由 AI 在对话中逐步与用户确认后生成。作为唯一事实源以结构化 JSON 落盘（项目根 `核心要素.json`），是设定生成等下游环节的输入依据。
_Avoid_: 设定、背景

**故事愿景文档**:
创意共创的展示层交付件（项目根 `故事愿景文档.md`），由 Agent 基于核心要素展开，含市场分析、人物框架、分卷节奏等给人读的补充内容。不是事实源，事实以核心要素 JSON 为准。
_Avoid_: 愿景文档、设定文档、策划文档

**设定**:
小说的背景资料总称，包含世界观、人物卡片、大纲三类。生成后用户可在前端查看和修改。
_Avoid_: 背景设定、设定集

**人物卡片**:
设定中的人物条目，含性格、目标、缺陷、金手指等结构字段，可编辑。
_Avoid_: 角色档案、人设

**前置设定完备检测**:
正文生成前对设定的完备性检查，不齐备则拦截并提示补齐。
_Avoid_: 前置检查

**正文**:
设定齐备后由 AI 生成的章节正文，用户可在阅读页阅读并请求局部修改。
_Avoid_: 章节、正文内容

**阅读页**:
用户阅读正文、选择段落并与 Agent 对话请求修改的页面。
_Avoid_: 编辑器、读者页

**审阅与去AI味**:
正文生成后的质量处理环节——审阅 + 去除 AI 味，创作流程的收尾步骤。
_Avoid_: 润色、质检

## Language (重构方向 · 2026-08-16 共同演进中)

**流程节点（创作状态，2026-08-18 正式定名）**:
每本小说所处的当前创作环节（灵感捕捉 / 世界观构建 / 人物塑造 / 大纲生成 / 正文生成 / 质量审查 / 文风优化）的演进。流程节点是 Agent 的「当前上下文标签」，不是强制关卡——用户可在任意节点与 Agent 交互，Agent 按节点加载相应上下文。旧称「状态 / 创作状态 / 状态演进周期」，后续统一叫「流程节点」。
_Avoid_: 硬编码的阶段、线性关卡

**两级状态**:
小说整体状态（当前所处创作环节）+ 每个工作单元（章节/设定/卷等）自身进度状态的聚合。工作单元有独立状态（如章节 PENDING→GENERATED→FINALIZED→REVIEWED）。
_Avoid_: 单一一层状态

**可扩展状态集**:
预置多个创作状态，但允许用户为每本书选择启用哪几个环节；状态是可扩展的，核心五个之外可增量增加工作流状态。
_Avoid_: 固定的五个状态

**通用 Agent 交互窗口**:
以 Agent 为流程中枢的统一对话界面，用户可在任意步骤与之交互。各状态的可视化界面是「当前要展示状态 + 产物查看/编辑的面板」，创作行为由 Agent 对话驱动。
_Avoid_: 六个彼此独立的工具页

**状态上下文组装**:
每个状态定义「该读哪些数据 + 按什么顺序注入」的规则；Agent 启动时按其组装上下文，并有 RAG 检索工具可自主按需召回（如伏笔是否兑现、人物说过什么）。
_Avoid_: 静态全量注入、无规则的随意读取

**单书记忆 RAG**:
以书为单位的正典事实、人物关系、伏笔网络、分层摘要的记忆库，写作时按需检索注入上下文。区别于跨书公共知识库。
_Avoid_: 跨书库检索

**统一 Agent 窗口（中枢）+ 状态内容窗口**:
交互中枢是统一的 Agent 对话窗口，Agent 驱动一切创作行为；同时保留 工作台 / 世界观 / 人物卡片 / 章纲 / 用户设置 等窗口，作为各状态的「内容面板」——状态的可视化 + 产物的查看/编辑。预设的风格/用户配置是旁路。并非推倒前端，而是把 Agent 提升为中枢、页面降格为面板。
_Avoid_: 单一聊天窗口砍掉所有界面、六个彼此独立驱动的工具页

**流程节点（state node，原状态节点）**:
小说级每个流程节点在数据模型中是携带行为的实体对象：`{key, label, panel, context_assembly_ref, enabled}`。键/标签、对应的「内容面板」、其「上下文组装」规则引用、以及该书是否启用该节点，由流程节点机统一调度。自由导航意味着节点之间无强制转移关系。
_Avoid_: 纯枚举字符串、带强制转移关系的关卡

**单锚点 + 进行中工作单元集合**:
小说整体当前聚焦**一个**「状态」（单锚点，Agent 窗口聚焦于此）；同时一本书始终存在一组**进行中的工作单元**（章节/设定/卷等，各自有进度状态）。两级状态由此耦合。
_Avoid_: 一次聚焦多个小说级状态

**创意孵化**:
由 IDEA 出发，把创意火花孵化成「可动笔」的设定起点——含核心要素确认（主线/愿景/风格/世界观，落盘 `核心要素.json`）+ 愿景展示的流程。对应旧「创意共创/IDEATION」。Agent 的 done=true 表示该阶段完成。
_Avoid_: 创业孵化、创意共创

**默认流程节点集（7 个，2026-08-18 更新）**:
完整 7 个默认流程节点（可扩展、每书选启用）：灵感捕捉 / 世界观构建 / 人物塑造 / 大纲生成（内部三级：总纲—分卷—章纲）/ 正文生成 / 质量审查 / 文风优化。旧 7 状态中「伏笔管理」移出流程节点、改为动态台账（见动态设定）；「文风优化」为新增节点，是用户主动触发的低频动作。旧 Project.status 枚举被这组节点直接改写。
_Avoid_: 固定五个、写作/reviewing 二分枚举、把伏笔管理当流程节点

**静态设定（故事宪法）**:
低频变更的设定层，由对应流程节点生成，**仅生成节点可改**。含 故事愿景文档 / 核心创意卡（灵感捕捉）、世界观信息（世界观构建）、人物设定 / 人物关系图（人物塑造）、大纲三级 + 故事线DAG（大纲生成）、风格规范（文风优化）。正文里的世界变化不改这里，而是记入动态设定。
_Avoid_: 把动态变化直接改写静态设定

**动态设定（世界真相层）**:
随故事推进而变的「活」状态，是 Agent 的全知真相层，由**正文生成**节点章末自动回写；界面只读（编辑需强提醒会影响上下文）。含 人物动态状态（含状态时间线）/ 地点与势力状态 / 物品状态 / 事件流 / 故事时间线 / 伏笔台账 / 信息与视角状态 / 章节摘要链。
_Avoid_: 读者视角信息与真相混存、用户直接编辑真相层

**信息/视角状态（真相层 vs 展示层）**:
独立常驻的动态账本，记录「世界实际发生的事实（真相）」与「读者当前被告知的信息（展示）」的差集。悬疑/宫廷等靠信息差驱动的题材直接受益；真相层仅供 Agent 全知视角使用，展示层约束正文只能透露到该披露的点。
_Avoid_: 真相与正文混杂、未区分读者已知信息

**大纲生成（三级结构）**:
大纲生成节点的标准输出件按三级落盘：总纲（起承转合/核心冲突/高潮结局）→ 分卷（各卷目标/阶段蓝图）→ 章纲（每章目标/情节点/伏笔安排），另附 故事线DAG。三级仍属同一个流程节点，界面可分卷/分章浏览。
_Avoid_: 把三级拆成三个流程节点

**文风优化（主动动作）**:
用户主动触发的低频动作，非自动环节：生成/校准风格规范（风格引擎、写作偏好、命名规范、反AI味规则），并按需对全书或选中章节做文风优化重写。触发后必须回写动态设定，避免正文与真相漂移。
_Avoid_: 每章自动重写、把文风优化做成自动流程环节


**单书记忆（四类）**:
以书为单位的记忆层四类：正典事实（原子、append-only，`memory/facts.jsonl`）/ 人物关系快照（json）/ 伏笔台账（append-only，`memory/foreshadow.jsonl`，埋/收/兑现）/ 分层摘要（`memory/summaries/L1..L5`）。记忆带 state 与 source 标签，供状态上下文组装与 RAG 检索。
_Avoid_: 跨书记忆、单一长文本记忆

**正典事实（facts）**:
append-only 的原子事实账本，每条一句话可独立断言，含来源与认知人物；更正以追加新条并标记旧条 superseded 实现，不覆盖。
_Avoid_: 段落级长文本、覆盖式改写

**伏笔台账（foreshadow ledger）**:
全书伏笔的埋/收/兑现状态（append-only），伏笔管理（横切状态）的依据，供正文/审阅状态按需召回"活跃伏笔"。
_Avoid_: 埋在章纲里不可检索、无兑现跟踪

**分层摘要（L1~L5）**:
渐进式摘要层级（L1 单章 → L5 全书），由状态机工作流落盘后自动触发沉淀，供上下文组装按需注入。
_Avoid_: 只存单层、依赖实时汇编


**轻量 RAG / MemoryRetriever**:
单书记忆的检索取舍：本轮不上向量库（维持 ADR-0001），用「规则关键词召回 + Agent 主动工具读取 + 分层摘要兜底」三管并用实现按需召回。暴露一篇 `MemoryRetriever` 接口，`MEMORY_RETRIEVAL_MODE` 可切换 keyword|vector——向量库为 Not-yet 可选项。
_Avoid_: 直接上 Qdrant/Chroma、把 embedding 当作唯一检索手段


**ContextOrchestrator（上下文编排器）**:
重写上下文管理的统一编排器，取代旧 ContextManager，全量负责 系统提示/记忆召回/拼接/压缩。内部三个协作对象：`PromptAssembler`（按状态规则拼注入块）、`MemoryRetriever`（轻量 RAG 召回）、`TokenCompressor`（预算/裁剪/滑动窗口/摘要，摘要与分层摘要 L1~L5 联动）。turn 前预注入 + 运行中 Agent 主动检索。
_Avoid_: 对话级滑动窗口替代、无状态感知的上下文


**统一 Agent 窗口（AgentWindow）**:
主导航常驻的对话窗口，对话驱动一切创作；内容区随当前状态显示对应产物面板。面板可点选、字段级可编辑；沿用升级 ask_user 结构化卡片；单持续对话线，切换状态时由 ContextOrchestrator 按新状态重组上下文（状态自动感知）。
_Avoid_: 每状态独立对话、只读无交互面板、把 ask_user 当普通消息


**Agent 独立页面（Agent 中枢页）**:
Agent 是前端的一个独立一等页面（主导航中枢对话），与 工作台 / 设定 / 正文 / 审阅 / 用户设置 等独立内容页并列存在，彼此不被包裹。Agent 通过感知当前状态 + 记忆/上下文协作。修订 T3 的"Agent 包裹内容区"形态。
_Avoid_: Agent 包裹所有页、各页各自嵌一套对话

**群聊模式（ChatSession）**:
多 Agent 剧情讨论的实时讨论房间：多个 Agent 角色与作者作为群成员自由发言，达成共识后由合成者产出最终方案，作者可随时手动终止。取代旧「标准模式」（并行产出 + 汇总）。见 `docs/design/multi-agent-chat-mode.md` 与 `docs/adr/0010-multi-agent-chat-mode.md`。
_Avoid_: 标准模式、并行产出 + 汇总

**发言意愿度**:
群聊模式发言权调度的核心量：被@(+100) + 等待时间(+0~30) + 话题相关性(+0~50，Embedding) + 角色特性(+0~20) − 冷却(−20~−50) + 随机(+0~10)。每轮选最高分者发言，保证有序、防垄断、防冷场。
_Avoid_: 固定轮流、随机发言

**共识检测（综合检测）**:
群聊模式的结束判定：关键词信号 + 观点收敛（向量聚类）+ Agent 自评 三路信号，连续 2 轮加权超阈值触发，由合成者产出总结。
_Avoid_: 固定轮数结束、单人拍板
